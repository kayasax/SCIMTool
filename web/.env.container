# Production configuration for containerized deployment
# Use empty base - client code adds /scim prefix automatically
VITE_API_BASE=
# Runtime token is supplied via the UI prompt; never embed secrets in the bundle.
VITE_SCIM_TOKEN=